---
export interface Props {
  data: {
    aboutMe: string;
  };
  heading?: string;
}

const { data, heading = "About" } = Astro.props;
---

<div class="rail-section">
  <span class="section-label">{heading}</span>
  <p class="about-text">{data.aboutMe}</p>
</div>

<style>
  .about-text {
    font-size: 0.85rem;
    line-height: 1.7;
    color: var(--text-secondary);
  }
</style>
